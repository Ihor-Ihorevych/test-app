<Window x:Class="test_app_src.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:test_app_src"
        mc:Ignorable="d"
        Title="DOCX Reader" Height="150" Width="300" ResizeMode="NoResize">
    <Grid VerticalAlignment="Center">
        <StackPanel Width="200" Height="Auto">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="5"/>
                </Style>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="Margin" Value="5"/>
                </Style>
            </StackPanel.Resources>
            <TextBox Text="Some text" />
            <Button Click="Open_File_Button_Handler" Content="Choose file"/>
            <Button Click="Start_Button_Handler" Content="Start"/>
        </StackPanel>
        
    </Grid>
</Window>
